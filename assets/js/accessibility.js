document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('a[target="_blank"]').forEach(a=>a.setAttribute('aria-label',`${a.textContent.trim()} (opens in a new tab)`));document.querySelectorAll('img:not([alt])').forEach(i=>i.alt='');const header=document.querySelector('.site-header');if(header)header.classList.add('is-sticky');let top=document.querySelector('.back-to-top');if(!top){top=document.createElement('button');top.className='back-to-top';top.type='button';top.setAttribute('aria-label','Back to top');top.textContent='↑ Top';document.body.appendChild(top)}const update=()=>top.classList.toggle('is-visible',window.scrollY>500);window.addEventListener('scroll',update,{passive:true});update();top.addEventListener('click',()=>window.scrollTo({top:0,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'}));});
