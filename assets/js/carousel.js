document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('.carousel');if(!root)return;const track=root.querySelector('.carousel-track'),slides=[...root.querySelectorAll('.carousel-slide')],status=root.querySelector('.carousel-status');let i=0,timer;const render=()=>{track.style.transform=`translate3d(${-i*100}%,0,0)`;if(status)status.textContent=`Learning visual ${i+1} of ${slides.length}`};const next=()=>{i=(i+1)%slides.length;render()};if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){track.style.transform='translate3d(0,0,0)';return}timer=setInterval(next,2000);root.addEventListener('mouseenter',()=>clearInterval(timer));root.addEventListener('mouseleave',()=>{clearInterval(timer);timer=setInterval(next,2000)});render()});